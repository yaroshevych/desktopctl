[package]
name = "desktopctl"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["std"] }
desktop-core = { path = "../core" }
serde_json = "1"
uuid = { version = "1", features = ["v7"] }
