[package]
name = "desktop-app"
version = "0.2.0"
edition = "2024"

[dependencies]
desktop-core = { package = "desktop-common", path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
